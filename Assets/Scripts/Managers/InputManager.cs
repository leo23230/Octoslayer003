using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//using UnityEngine.InputSystem;

public class InputManager : MonoBehaviour
{
 /*   static InputManager instance;

    [Header("Set Bindings")]
    public InputAction _myAction;
    public InputAction _myAction2;
    public InputAction _myAction3;


    public static InputAction myAction;
    public static InputAction myAction2;
    public static InputAction myAction3;

    void Awake()
    {
        // make singleton
        if (instance == null)
        {
            instance = this;
            DontDestroyOnLoad(this);

            // assign static actions to reference the local ones
            myAction = _myAction;
            myAction2 = _myAction2;
            myAction3 = _myAction3;
        }
        else
        {
            Destroy(gameObject);
        }
    }

    // enable all actions
    void OnEnable()
    {
        _myAction.Enable();
        _myAction2.Enable();
        _myAction3.Enable();
    }

    // disable all actions
    void OnDisable()
    {
        _myAction.Disable();
        _myAction2.Disable();
        _myAction3.Disable();
    }*/
}
